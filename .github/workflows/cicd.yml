name: CI/CD

on:
  pull_request:
    branches:
      - master
      - stage
  push:
    branches:
      - master
      - stage

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Run linting
        run: pnpm lint

      - name: Run type checking
        run: pnpm typecheck

  deploy:
    name: Deploy
    needs: lint
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/master' && 'production' || 'preview' }}
    permissions:
      contents: read
      deployments: write

    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm run build
        env:
          NUXT_GIT_COMMIT: ${{ github.sha }}
          NUXT_BUILD_DATE: ${{ github.event.head_commit.timestamp }}
          STRAPI_URL: ${{ vars.STRAPI_URL }}
          CLAUDE_KEY: ${{ secrets.CLAUDE_KEY }}
          SENDGRID_KEY: ${{ secrets.SENDGRID_KEY }}
          MAILER_LITE_KEY: ${{ secrets.MAILER_LITE_KEY }}
          LAWMATICS_URL: ${{ vars.LAWMATICS_URL }}
          LAWMATICS_TOKEN: ${{ secrets.LAWMATICS_TOKEN }}
          LAWMATICS_QUIZ_FORM_URL: ${{ vars.LAWMATICS_QUIZ_FORM_URL }}
          ME_SEARCH_KEY: ${{ secrets.ME_SEARCH_KEY }}
          GTAG_ID: ${{ vars.GTAG_ID }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_KEY: ${{ secrets.CLOUDFLARE_API_KEY }}
          CLOUDFLARE_ACCESS_KEY_ID: ${{ vars.CLOUDFLARE_ACCESS_KEY_ID }}
          CLOUDFLARE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_SECRET_ACCESS_KEY }}
          CLOUDFLARE_BUCKET_NAME: ${{ vars.CLOUDFLARE_BUCKET_NAME }}
          FIREBASE_API_KEY: ${{ vars.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ vars.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}
          DOCX_CONVERTER_URL: ${{ vars.DOCX_CONVERTER_URL }}
          DOCX_CONVERTER_KEY: ${{ secrets.DOCX_CONVERTER_KEY }}
          MAILERSEND_KEY: ${{ secrets.MAILERSEND_KEY }}

      - name: Verify build output
        run: |
          if [ -f .output/server/index.mjs ]; then
            echo "✓ Worker script generated successfully"
          else
            echo "✗ Worker script not found!"
            exit 1
          fi
          if [ -d .output/public/_nuxt ]; then
            echo "✓ Static assets generated successfully"
          else
            echo "✗ Static assets not found!"
            exit 1
          fi

      - name: Deploy to Preview
        if: github.ref == 'refs/heads/stage'
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: .output
          command: deploy --env preview

      - name: Deploy to Production
        if: github.ref == 'refs/heads/master'
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: .output
          command: deploy
